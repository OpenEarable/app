/// A record value consists of the minimum and the maximum value along one axis of measurement
class RecordValue {
  final int min;
  final int max;

  RecordValue({required this.min, required this.max});
}
